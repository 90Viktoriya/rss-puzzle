import { BaseComponent } from '../components/baseComponent';
import './mainPage.css';

export const mainPage = () => new BaseComponent({ tag: 'div', className: 'mainPage_wrapper' });

export default mainPage;
